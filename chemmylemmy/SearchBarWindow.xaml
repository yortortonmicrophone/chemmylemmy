<Window x:Class="chemmylemmy.SearchBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SearchBar" Width="420"
        WindowStyle="None" ResizeMode="NoResize" Topmost="True" ShowInTaskbar="False" Background="{StaticResource WindowBackgroundBrush}" AllowsTransparency="True" Opacity="1.0" SizeToContent="Height"
        FontFamily="Cascadia Mono, Consolas, 'Segoe UI', monospace">
    <!-- No local resources needed; all brushes/colors are now in App.xaml -->
    <Border CornerRadius="7" Padding="8" SnapsToDevicePixels="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{StaticResource WindowBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>
        <StackPanel>
            <Border CornerRadius="5" Background="{StaticResource SearchBoxBackgroundBrush}" Margin="0,0,0,6" SnapsToDevicePixels="True" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
                <TextBox x:Name="SearchTextBox" Height="26" FontSize="13" VerticalContentAlignment="Center"
                         Background="Transparent" Foreground="{StaticResource TextBrush}" BorderThickness="0" Padding="6,0"
                         CaretBrush="{StaticResource AccentBrush}" SelectionBrush="{StaticResource HighlightBrush}"/>
            </Border>
            <Border CornerRadius="4" Background="{StaticResource ResultsBoxBackgroundBrush}" SnapsToDevicePixels="True" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0">
                <TextBlock x:Name="ResultsTextBlock" Foreground="{StaticResource TextBrush}" FontSize="12" Text="Results will appear here..."
                           Padding="8,6,8,6" Margin="0" TextWrapping="Wrap" VerticalAlignment="Top"/>
            </Border>
        </StackPanel>
    </Border>
</Window> 